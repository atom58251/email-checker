"use client";

export const dynamic = "force-dynamic";

import { useRole } from "@/lib/useRole";

type StatusRow = { status: string; tag: "ok" | "del" | "review"; meaning: string; duration: string };

const keepStatuses: StatusRow[] = [
  {
    status: "OK",
    tag: "ok",
    meaning: "Синтаксис верный, домен существует и принимает почту (найдена MX-запись, либо неявный MX через A-запись — RFC 5321).",
    duration: "Не блокируется. Результат нигде не сохраняется — при следующей проверке домен резолвится заново.",
  },
];

const deleteStatuses: StatusRow[] = [
  {
    status: "INVALID_SYNTAX",
    tag: "del",
    meaning: "Некорректная структура адреса.",
    duration: "Не хранится централизованно — пересчитывается заново при каждой проверке. Результат детерминирован (невалидный синтаксис сам по себе не исправится), поэтому по факту это блокировка навсегда, хотя технически не в базе.",
  },
  {
    status: "EMPTY",
    tag: "del",
    meaning: "Пустая ячейка.",
    duration: "Аналогично — пересчитывается локально, не хранится.",
  },
  {
    status: "TYPO_SUSPECTED",
    tag: "del",
    meaning: "Похоже на опечатку в известном домене (например, gmial.com). В комментарии — предполагаемый верный вариант.",
    duration: "Не хранится централизованно. Список известных доменов для сравнения зашит в код сервиса.",
  },
  {
    status: "DELETE_DISPOSABLE",
    tag: "del",
    meaning: "Домен — известный одноразовый почтовый сервис (mailinator и т.п.).",
    duration: "Не хранится централизованно. Чёрный список доменов зашит в код, меняется только при обновлении сервиса.",
  },
  {
    status: "DELETE_SUPPRESSED",
    tag: "del",
    meaning: "Адрес уже есть в базе известных мёртвых — подтверждено реальной недоставкой ранее.",
    duration:
      "Зависит от типа записи: постоянная (адрес не существует, spam trap) — навсегда; spam trap — навсегда, не смягчается повторным импортом; временная (например, mailbox full) — 28 дней от даты последнего подтверждения, затем адрес снова участвует в проверках.",
  },
  {
    status: "DELETE_DOMAIN_NOT_EXISTS",
    tag: "del",
    meaning: "Домен не существует — подтверждено независимо двумя DNS-резолверами (Google + Cloudflare).",
    duration:
      "Кэшируется на 90 дней для ВСЕХ пользователей сразу (не только для вас) — повторная проверка того же домена кем угодно в этот срок не идёт в сеть. После 90 дней домен перепроверяется заново.",
  },
];

const reviewStatuses: StatusRow[] = [
  {
    status: "NO_MAIL_SUSPECTED",
    tag: "review",
    meaning: "Домен существует, но нет ни MX-, ни A-записи — необычно, но не стопроцентное доказательство мёртвого домена.",
    duration: "Не блокируется и не кэшируется — резолвится заново при каждой проверке.",
  },
  {
    status: "DNS_INCONCLUSIVE",
    tag: "review",
    meaning: "DNS-резолвер(ы) не дали содержательного ответа (технический сбой, таймаут, недоступность резолвера).",
    duration: "Не блокируется и не кэшируется — временный технический сбой, пробуется заново при следующей проверке.",
  },
  {
    status: "DNS_INCONSISTENT",
    tag: "review",
    meaning: "Два резолвера дали два разных уверенных ответа (например, один говорит «домена нет», другой — что домен существует).",
    duration: "Не блокируется и не кэшируется. Встречается редко — стоит присмотреться к DNS-настройке домена вручную, если повторяется часто.",
  },
];

function StatusTable({ rows }: { rows: StatusRow[] }) {
  return (
    <div className="table-wrap">
      <table>
        <thead>
          <tr>
            <th>Статус</th>
            <th>Значение</th>
            <th>Срок / механизм</th>
          </tr>
        </thead>
        <tbody>
          {rows.map((r) => (
            <tr key={r.status}>
              <td>
                <span className={`tag ${r.tag}`}>{r.status}</span>
              </td>
              <td>{r.meaning}</td>
              <td className="muted">{r.duration}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}

export default function HelpPage() {
  const { role, loading } = useRole();
  const isAdmin = role === "admin";

  if (loading) return <div className="container">Загрузка...</div>;

  return (
    <div className="container help-container">
      <h1>📧 Инструкция по сервису</h1>
      <p className="muted">
        {isAdmin
          ? "Полное руководство: как пользоваться сервисом для проверки списков рассылки и как администрировать базу известных недействительных адресов."
          : "Руководство по проверке списков email перед рассылкой."}
      </p>

      <div className="help-nav">
        <a href="#overview">Обзор</a>
        <a href="#user">Как проверить список</a>
        <a href="#statuses">Статусы</a>
        {isAdmin && <a href="#admin">Администрирование</a>}
        <a href="#security">Безопасность</a>
        <a href="#faq">Вопросы</a>
        <a href="/">← На главную</a>
      </div>

      <h2 id="overview">1. Что это за сервис</h2>
      <p>
        Проверка списков email перед рассылкой — без риска для репутации вашего отправляющего
        сервера. Сервис <strong>никогда не подключается к серверам получателей писем</strong>{" "}
        (никакого SMTP RCPT TO) — именно такая проверка демаскирует ваш IP как источник массовых
        проверок и может привести к блокировке. Вместо этого используются:
      </p>
      <ul>
        <li>синтаксис адреса — локально, мгновенно;</li>
        <li>опечатки в популярных доменах — сравнение строк, локально;</li>
        <li>одноразовые почтовые сервисы — сверка по списку известных доменов;</li>
        <li>DNS-проверка MX-записи через Google/Cloudflare DNS-over-HTTPS — вопрос к DNS, не подключение к почтовому серверу;</li>
        <li>база уже известных мёртвых адресов{isAdmin ? " — пополняется вами из реальных отчётов о недоставке" : ""}.</li>
      </ul>

      <div className="table-wrap">
        <table>
          <thead>
            <tr>
              <th>Действие</th>
              <th>Где выполняется</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>Разбор файла, синтаксис, опечатки, disposable-домены</td>
              <td>В браузере пользователя (мгновенно, без лимитов сервера)</td>
            </tr>
            <tr>
              <td>Сверка с базой известных мёртвых адресов</td>
              <td>Сервер, по хэшу — сами адреса на сервере в открытом виде не хранятся</td>
            </tr>
            <tr>
              <td>Проверка MX-записи домена</td>
              <td>Сервер, через Google/Cloudflare DNS-over-HTTPS</td>
            </tr>
          </tbody>
        </table>
      </div>

      <h2 id="user">2. Как проверить список email</h2>

      <h3>2.1 Вход</h3>
      <p>
        Учётную запись создаёт администратор сервиса — самостоятельная регистрация отключена.
        Если у вас нет доступа, обратитесь к администратору за email и паролем.
      </p>

      <h3>2.2 Как загрузить список на проверку</h3>
      <ol className="steps">
        <li>В поле «Название колонки с email» укажите название колонки в вашем файле (по умолчанию — <code>email</code>). Регистр и пробелы не важны.</li>
        <li>Нажмите «Выберите файл» — .xlsx или .csv.</li>
        <li>Нажмите «Загрузить и проверить».</li>
        <li>Индикатор покажет стадию: разбор файла → офлайн-проверки → сверка с базой мёртвых адресов → проверка доменов → сборка результата.</li>
      </ol>
      <div className="card">
        <strong>Большие файлы (100 000+ строк)</strong> обрабатываются за секунды-минуты — вся
        тяжёлая часть работы идёт в вашем браузере, а не на ограниченном по времени сервере.
      </div>

      <h3>2.3 Результат проверки</h3>
      <p>
        В «Мои проверки» появится строка с файлом. Когда статус — «готово», в колонке «Удалить /
        Проверить / Оставить» будет разбивка по трём группам (подробности ниже). «Скачать» отдаёт
        исходный файл с добавленными колонками <code>status</code> и <code>comment</code>.
      </p>
      <p className="muted">Ссылка на скачивание действует 60 секунд — если не успели, нажмите «Скачать» ещё раз.</p>

      <h3>2.4 Хранение данных</h3>
      <p>Файлы и результаты автоматически удаляются через 14 дней. Чужие проверки не видны — доступ разграничен на уровне базы данных.</p>

      <h2 id="statuses">3. Что означают статусы результата</h2>
      <p>Каждому адресу присваивается один из статусов — вот что каждый значит и на какой срок действует.</p>

      <h3>Оставить в рассылке</h3>
      <StatusTable rows={keepStatuses} />

      <h3>Удалить из списка</h3>
      <StatusTable rows={deleteStatuses} />

      <h3>Требуют вашего решения (не удаляются автоматически)</h3>
      <StatusTable rows={reviewStatuses} />

      <div className="card">
        <strong>Главное правило:</strong> «навсегда» блокируют только постоянные записи в базе
        мёртвых адресов (включая все spam trap) и подтверждённо несуществующие домены (с
        перепроверкой раз в 90 дней). Всё остальное либо пересчитывается каждый раз заново, либо
        истекает через 28 дней.
      </div>
      {!isAdmin && (
        <p className="muted">
          Конкретный несуществующий почтовый ящик на живом домене (например, опечатка в имени
          пользователя на gmail.com) можно обнаружить только реальной отправкой — сервис
          намеренно не «стучится» к серверам получателей, чтобы не рисковать репутацией
          отправителя. Такие адреса попадают в базу известных мёртвых после того, как
          администратор загрузит отчёт о реальной недоставке.
        </p>
      )}

      {isAdmin && (
        <>
          <h2 id="admin">4. Администрирование</h2>

          <h3>4.1 Импорт отчёта о недоставке</h3>
          <p>
            На странице <code>/admin</code> — одна форма загрузки: колонка email (необязательно) и
            сам файл (.csv или .xlsx). Правила обработки одни и те же для любого источника отчёта.
          </p>
          <ul>
            <li>Адрес ищется в колонках <code>email</code>, <code>address</code>, <code>recipient</code>, <code>recipient_email</code>, либо своё имя вручную.</li>
            <li>Если отдельной колонки нет — адрес извлекается из текста ответа сервера (<code>smtp_response</code> или <code>last_response</code>), там где провайдер его публикует.</li>
            <li>Автоматическая защита от строки <code>sep=;</code> в начале файла (артефакт Excel).</li>
            <li>Обязательна только колонка <code>category</code>.</li>
          </ul>

          <h3>4.2 Как классифицируются категории</h3>
          <div className="table-wrap">
            <table>
              <thead>
                <tr>
                  <th>Группа</th>
                  <th>Что значит</th>
                  <th>Что происходит</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td><strong>PERMANENT_BLOCK</strong></td>
                  <td>Адрес точно мёртв (не существует, домен не принимает почту и т.п.)</td>
                  <td>В базу навсегда</td>
                </tr>
                <tr>
                  <td><strong>TRAP</strong></td>
                  <td>Spam trap / одноразовый ящик, обнаруженный по факту доставки</td>
                  <td>В базу навсегда, флаг is_trap — не смягчается повторным импортом</td>
                </tr>
                <tr>
                  <td><strong>RETRY_LATER</strong></td>
                  <td>Временная проблема (например, переполнен ящик)</td>
                  <td>В базу, блокирует 28 дней с момента последнего подтверждения</td>
                </tr>
                <tr>
                  <td><strong>SENDER_ISSUE</strong></td>
                  <td>Проблема на стороне вашей отправки (throttling, недоступность сервера, отказ по политике)</td>
                  <td>Не сохраняется вообще</td>
                </tr>
              </tbody>
            </table>
          </div>

          <p className="muted">
            Уже поддерживаются формулировки: address does not exist, invalid_mailbox,
            domain_has_no_mail_server, broken mx record, Yandex account blocked by Yandex,
            disposable inbox service → PERMANENT_BLOCK; disposable_or_trap → TRAP; mailbox full /
            mailbox_full → RETRY_LATER; mail_server_unreachable, server_refuses_mail,
            gmail_throttling → SENDER_ISSUE.
          </p>
          <div className="card">
            Категория, которой нет в списке, по умолчанию считается SENDER_ISSUE (пропускается) —
            безопаснее проверить незнакомую формулировку вручную, чем автоматически удалить живой
            адрес.
          </div>

          <h3>4.3 Что видно после импорта</h3>
          <ul>
            <li>Сколько адресов добавлено/обновлено, взято из колонки vs извлечено из текста ответа, сколько не распознано</li>
            <li>Сколько обнаружено spam trap (отдельное предупреждение)</li>
            <li>Сводка по пропущенным SENDER_ISSUE-категориям, с отдельным предупреждением при <code>gmail_throttling</code></li>
          </ul>

          <h3>4.4 Кэш подтверждённых мёртвых доменов</h3>
          <p>
            Отдельно от базы адресов сервис ведёт кэш доменов (90 дней), для которых DNS-проверка
            при обычной проверке ЛЮБОГО пользователя независимо подтвердила отсутствие домена —
            это происходит автоматически, без вашего участия. Кэш содержит только имена доменов,
            без email-адресов.
          </p>

          <h3>4.5 Управление пользователями</h3>
          <p>
            Регистрация через форму отключена. Новых пользователей создаёт администратор через
            Supabase Dashboard (Authentication → Users → Add user, с включённым Auto Confirm) или
            через SQL, и сообщает им пароль отдельно. Роль admin назначается там же через SQL:
          </p>
          <pre>
            <code>{`update public.profiles set role = 'admin'
where id = (select id from auth.users where email = 'нужный_email@пример.com');`}</code>
          </pre>
        </>
      )}

      <h2 id="security">5. Безопасность и приватность</h2>
      <div className="table-wrap">
        <table>
          <thead>
            <tr>
              <th>Мера</th>
              <th>Зачем</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>База мёртвых адресов хранит SHA-256 хэш, не сам email</td>
              <td>Даже при утечке таблицы восстановить исходные адреса невозможно</td>
            </tr>
            <tr>
              <td>Row Level Security на всех таблицах</td>
              <td>Нельзя увидеть чужие проверки через API, даже в обход интерфейса</td>
            </tr>
            <tr>
              <td>Ссылка на скачивание — временная (60 секунд)</td>
              <td>Нет постоянной публичной ссылки на файл</td>
            </tr>
            <tr>
              <td>Автоудаление файлов и результатов через 14 дней</td>
              <td>Минимизация хранения данных</td>
            </tr>
            <tr>
              <td>Никаких SMTP-подключений к серверам получателей</td>
              <td>Не создаёт риска блокировки IP отправителя</td>
            </tr>
          </tbody>
        </table>
      </div>

      <h2 id="faq">6. Частые вопросы</h2>

      <h4>«Ошибка разбора CSV: Unable to auto-detect delimiting character»</h4>
      <p>Предупреждение, не ошибка — обычно означает, что в файле одна колонка. Данные разбираются верно.</p>

      <h4>«В файле нет колонки email»</h4>
      <p>Сообщение покажет реальный список колонок из файла — впишите точное название в поле формы.</p>

      {isAdmin && (
        <>
          <h4>Почему обычная проверка пользователя не пополняет базу мёртвых адресов?</h4>
          <p>
            Осознанное разделение по типу данных. Домены, которых физически не существует
            (подтверждено DNS), кэшируются автоматически из ЛЮБОЙ проверки — это не требует вас.
            А конкретные несуществующие почтовые ящики на живых доменах может обнаружить только
            реальная попытка отправки — это узнаётся исключительно из отчётов о недоставке,
            которые загружаете вы.
          </p>
        </>
      )}

      <h4>Почему адрес с «mailbox full» не удаляется навсегда?</h4>
      <p>Переполненный ящик — временная ситуация. Такие адреса блокируются на 28 дней, затем снова участвуют в проверках.</p>

      <p className="muted" style={{ marginTop: 40 }}>
        Email Checker — сервис проверки email-списков перед рассылкой. Обработка списков — в
        браузере, сервер — только для сверки с базой известных адресов и проверки DNS.
      </p>
    </div>
  );
}
